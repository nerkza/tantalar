server:
  host: 0.0.0.0
  port: 8790
database:
  dialect: sqlite
  sqlite:
    path: /data/tantalar.db
plugins:
  set:
    dev.tantalar.plugin.metadata-tmdb-tvdb:
      config:
        metadataGatewayUrl: https://metadata-staging.tantalar.app/v1/tmdb
